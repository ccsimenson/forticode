import { describe, it, expect } from 'vitest';

// Simple test to verify Vitest is working with TypeScript
describe('Example Test Suite', () => {
  it('should pass a simple test', () => {
    expect(1 + 1).toBe(2);
  });
});
